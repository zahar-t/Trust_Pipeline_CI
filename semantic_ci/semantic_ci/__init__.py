"""semantic-ci: Metric regression testing for dbt projects.

Snapshot metric values, diff them across runs, and gate CI pipelines
when metrics drift beyond expected thresholds.
"""

__version__: str = "0.1.0"
